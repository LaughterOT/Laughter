
//---- TASK ONE ----

let a = 3;
let b = 5;
let c ;

let result = `
a + b = ${a + b};

a - b = ${a - b};

a * b = ${a * b};

a / b = ${a / b};

a % b = ${a % b};

a += b = ${a += b};

a -= b = ${a -= b};

a *= b = ${a *= b};

a /= b = ${a /= b};

a %= b = ${a %= b};

a == b = ${a == b};

a != b = ${a != b};

a > b = ${a > b};

a < b = ${a < b};

!a && !c = ${!a && !c};

!a || !c = ${!a || !c};

`
console.log(result);

// ---- TASK TWO ---- 

let first_name = "Laughter";
let last_name = "Otulana";
let email = "otul0001@algonquinlive.com";

console.log ( `My name is ${first_name} ${last_name}. You can contact me at ${email}.`);



